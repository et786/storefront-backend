describe("placeholder", () => {
    it("expect to be shown in console", () => {
        // trivial test
        expect(1).toBe(1)
    })
})

describe("not placeholder", () => {
    it("expect to be shown", () =>{

    })
})