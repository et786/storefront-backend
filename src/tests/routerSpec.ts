import app from "../server";
import supertest from "supertest";

describe("routes spec placeholder", () => {
  it("expect to be shown in console", () => {
    // trivial test
    expect(1).toBe(1);
  });
});
